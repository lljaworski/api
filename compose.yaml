services:
  database:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password
      # MYSQL_DATABASE removed - database will be created by Symfony command
    ports:
      - "3306:3306"
    tmpfs: /var/lib/mysql
